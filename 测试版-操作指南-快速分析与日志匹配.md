# 操作指南：快速分析 — 从原始日志到负责人定位

> 适用版本：v1.3.1 / 引擎版本：logMatch v2.0  
> 更新日期：2026-07-17

---

## 概述

"快速分析"是本工具的核心功能：**将运行时日志中的异常行，与预先准备的模块日志文件进行匹配，再通过模块负责人表反查责任人**，最终输出"谁负责什么模块的什么异常"。

### 前置依赖

使用快速分析前，必须完成以下准备（详见《操作指南-关键词配置与数据管理》）：

| 序号 | 前提条件 | 产出物 | 存储位置 |
|:--:|----------|--------|----------|
| ① | 代码检索 → 导出 JSON | **模块日志文件**（CodeSearchResult 数组） | `config/<项目>/code-search/*.json` |
| ② | 数据管理 → 维护映射表 | **模块负责人文件**（ModuleMapping 数组） | `config/<项目>/module-mapping/mappings.json` |
| ③ | 打开要分析的运行时日志 | 主日志内容（log.txt） | 任意路径 |

## 第一部分：操作步骤

### 1.1 打开运行时日志

1. 启动应用，完成 **模块日志文件**和**模块负责人文件**的导入
2. 工具栏 → **📁 打开文件**
3. 选择要分析的日志文件（`.log` / `.txt` / `.log.1` 等）
4. 日志内容在主视图中显示

### 1.2 打开快速分析面板

1. 工具栏 → **🔗 快速分析** 按钮
2. 面板以浮动窗口形式出现在界面右侧

### 1.3 选择要参与匹配的模块

面板中显示当前项目下所有已导入的模块日志：

```
☑ job_manager      (48 条 | 7/16)
☑ video_pipeline   (77 条 | 7/16)
☐ scan_mfp         (74 条 | 7/16)
☐ scan_pu          (78 条 | 7/16)
...
```

- 单击模块行切换选中/取消
- **全选** 按钮一键选中所有模块
- 仅选中的模块参与匹配

### 1.4 执行匹配

1. 勾选目标模块后，面板底部出现 **🔍 开始匹配（N 个模块）** 按钮
2. 单击执行匹配
3. 匹配过程中显示"正在快速分析…"遮罩，完成后自动展示结果

### 1.5 查看匹配结果

匹配结果按**负责人**分组展示，不关心模块划分，只关注"谁名下的代码·函数产生了哪些错误日志"。

**结果头部：负责人概览**

```
👥 异常负责人：
  👤 张亮 — 3 处错误（2 个文件·函数）
  👤 李娜 — 1 处错误（1 个文件·函数）
  👤 未分配负责人 — 1 处错误（1 个文件·函数）
```

**结果详情：按人分组的匹配明细**

```
👤 张亮 — 3 处错误（2 个文件·函数）
  ▸ video_pcie.c · video_pcie_cancel_process（2处）
    行 649: video_pcie_cancel_process - [ERROR]L649 :[VIDEO] CANCEL
    行 651: video_pcie_cancel_process - [ERROR]L651 :[VIDEO] CANCELLED
  ▸ storage_dir.c · storage_if_dir_access（1处）
    行 32: storage_interface_file_dir_access- module/storage/storage_dir.c L32 [HAL][ERROR] --> stat failed

👤 李娜 — 1 处错误（1 个文件·函数）
  ▸ scan_mfp.c · scan_start（1处）
    行 200: scan_start - [ERROR]L200 :[SCAN] init failed
```

单击 ▶ 展开/折叠文件·函数项，单击 **行号标签**（如 `行 649`）直接跳转到日志主视图的对应行。

### 1.6 复制结果

单击 **📋 复制** 按钮，将完整分析结果复制到剪贴板（含负责人、统计、日志行详情）。

---

### 1.7 匹配结果复制格式

单击 **📋 复制** 后的剪贴板格式（按负责人分组）：

```
📋 快速分析结果

🕐 分析时间：2026-07-17 15:30:00

👥 负责人概览：
  👤 张伟 — 3 处错误（2 个文件·函数）
  👤 刘强 — 2 处错误（1 个文件·函数）

═══════════════════════════════════════

👤 张伟

  ▸ video_pcie.c · video_pcie_cancel_process（2处）
    行 649: video_pcie_cancel_process - [ERROR]L649 :[VIDEO] CANCEL
    行 651: video_pcie_cancel_process - [ERROR]L651 :[VIDEO] CANCELLED

  ▸ job_manager.c · jobRun（1处）
    行 4: jobRun - [ERROR][IPSLIB][1192] Pipeline SNIFFER failed

👤 刘强

  ▸ audio_player.cpp · playAudio（2处）
    行 200: playAudio - [ERROR]L200 :[AUDIO] buffer underrun
    行 301: playAudio - [ERROR]L301 :[AUDIO] device not found

═══════════════════════════════════════
由 Log Analyzer 生成
```

## 第二部分：完整示例 — 从原始日志到分析结论

### 场景

一份运行时日志 `log.txt` 包含数千行，需要定位其中的错误并确定责任人。

### 步骤详解

**Step 1：打开日志**

```
工具栏 → 📁 打开文件 → 选择 log.txt
```

日志内容示例：

```
[2026-07-15 10:23:17.372] [10:20:27.064] jobRun - [INFO][IPSLIB][1192][jobRun]job->namePipeline[SNIFFER]
[2026-07-15 10:23:17.372] [10:20:27.064] video_pcie_cancel_process - [ERROR]L649 :[VIDEO] CANCEL
[2026-07-15 10:23:17.372] [10:20:27.064] storage_interface_file_dir_access- module/storage/storage_dir.c L32 [HAL][ERROR] --> stat failed
[2026-07-15 10:23:17.372] [   0:21:12.350][INFO] /home/.../CViewModelBase.cpp L176 DispatchEvent():pedk_serv_mgr
```

**Step 2：确认项目配置**

```
工具栏 → 🔄 数据管理 → 确认当前项目已配置
  ├── 模块负责人表 ≥ 1 条
  └── 模块日志表 ≥ 1 个文件
```

**Step 3：执行快速分析**

```
工具栏 → 🔗 快速分析
→ 勾选 job_manager, video_pipeline, hal_module
→ 🔍 开始匹配（3 个模块）
```

**Step 5：结果呈现**

```
👥 异常负责人：
  👤 刘强 — 1 处错误（1 个文件·函数）

👤 刘强 — 1 处错误（1 个文件·函数）
  ▸ video_pcie.c · video_pcie_cancel_process（1处）
    行 649: video_pcie_cancel_process - [ERROR]L649 :[VIDEO] CANCEL
```

---

## 第三部分：关键注意事项与异常处理

### 快速分析不成功的常见原因

| 现象 | 可能原因 | 解决方案 |
|------|----------|----------|
| "请先导入模块映射表" | 当前项目无模块负责人数据 | 在数据管理面板导入或新增映射 |
| "未在日志中找到匹配" | 日志无 ≥ERROR 行，或函数名不匹配 | 确认日志含 `[ERROR]` 行，确认函数名与实际代码一致 |
| "请检查映射表配置" | 匹配到代码文件但无对应 codePath | 在映射表中补充对应文件的映射 |
| 匹配结果偏少 | 被选模块中无对应函数名候选 | 确认代码检索已覆盖该模块 |
| 负责人缺失 | codePath 路径段不匹配 | 确保 codePath 是实际文件路径的子串（含 `/` 边界） |
| WARNING 行被忽略 | 仅 ≥ERROR 进入匹配 | 预期行为，WARNING 等信息需升级或手动查看 |

