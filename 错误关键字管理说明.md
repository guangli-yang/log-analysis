# 错误关键字管理功能使用说明

## 功能概述

日志分析工具提供了完整的错误关键字管理功能，允许您自定义错误关键字及其对应的提示信息。

## 如何使用

### 1. 打开配置界面

1. 启动应用
2. 打开任意日志文件
3. 点击右侧错误分析面板顶部的 **⚙️** 按钮
4. 进入"错误关键字配置"模式

### 2. 添加新的错误关键字

在配置界面中：

1. 在"关键字"输入框中输入错误关键字（如：`database_err`）
2. 在"描述信息"输入框中输入对应的提示信息
   - 格式：`该错误为XXX模块，请找某某团队某部分某模块分析`
   - 示例：`该错误为数据库模块，请找数据库团队分析`
3. 点击 **「添加」** 按钮

### 3. 编辑现有的错误关键字

1. 在关键字列表中找到要编辑的条目
2. 点击 **✏️**（编辑）按钮
3. 输入框会显示当前的关键字和描述
4. 修改需要的内容
5. 点击 **✓**（保存）按钮保存修改
6. 或点击 **✗**（取消）按钮放弃修改

### 4. 删除错误关键字

1. 在关键字列表中找到要删除的条目
2. 点击 **🗑️**（删除）按钮
3. 该关键字将从列表中移除

### 5. 导出配置

1. 在配置界面底部点击 **「导出配置」** 按钮
2. 选择保存位置
3. 配置将保存为 JSON 文件
4. 可以备份或分享给其他用户

## 预设错误关键字列表

应用内置了以下预设错误关键字：

| 关键字 | 描述信息 |
|--------|----------|
| `print_err` | 该错误为打印模块，请找打印团队分析 |
| `copy_err` | 该错误为复制模块，请找文件传输团队分析 |
| `scan_err` | 该错误为扫描模块，请找扫描团队分析 |
| `error` | 通用错误，请检查日志上下文 |
| `exception` | 异常抛出，请找开发团队分析 |
| `fatal` | 致命错误，请立即联系运维团队 |
| `fail` | 操作失败，请检查相关模块 |
| `warning` | 警告信息，请留意相关日志 |

## 自动检测功能

当您打开日志文件时：

1. 系统会自动扫描日志内容
2. 检测所有预设的错误关键字
3. 如果检测到错误，错误分析面板会自动展开
4. 面板顶部会显示检测到的错误数量
5. 每个错误会显示：
   - 所在行号
   - 关键字
   - 对应的描述信息
   - 日志上下文

## 数据持久化

- 所有错误关键字配置会自动保存到浏览器本地存储
- 关闭应用后重新打开，配置仍然保留
- 无需手动保存

## 示例场景

### 场景1：添加新的错误类型

您的应用中出现了新的错误类型 `network_err`，需要添加：

1. 打开配置界面（点击⚙️）
2. 输入关键字：`network_err`
3. 输入描述：`该错误为网络模块，请找网络团队分析`
4. 点击「添加」
5. 完成！以后打开包含 `network_err` 的日志会自动显示该描述

### 场景2：修改现有描述

需要修改 `print_err` 的描述信息：

1. 打开配置界面
2. 找到 `print_err` 条目
3. 点击✏️按钮
4. 修改描述为：`该错误为打印服务模块，请找打印服务团队分析`
5. 点击✓保存
6. 完成！

### 场景3：删除不需要的关键字

不再需要 `warning` 关键字：

1. 打开配置界面
2. 找到 `warning` 条目
3. 点击🗑️按钮
4. 完成！`warning` 不再会被检测

## 界面说明

### 错误分析面板（未展开）
```
┌─────────────────────────┐
│ ⚠️ 错误分析 [3] │
└─────────────────────────┘
```

### 错误分析面板（已展开，未检测到错误）
```
┌─────────────────────────┐
│ ⚠️ 错误分析       │
│ [⚙️] [◀]         │
│                     │
│   ✅ 未检测到错误   │
└─────────────────────────┘
```

### 错误分析面板（已展开，检测到错误）
```
┌─────────────────────────┐
│ ⚠️ 错误分析 [3]   │
│ [⚙️] [◀]         │
│                     │
│ 检测到 3 个错误    │
│                     │
│ ┌───────────────────┐ │
│ │ 行 6              │ │
│ │ print_err         │ │
│ │ 该错误为打印...  │ │
│ │ 2024-01-15...   │ │
│ └───────────────────┘ │
│                     │
│ ┌───────────────────┐ │
│ │ 行 8              │ │
│ │ copy_err          │ │
│ │ 该错误为复制...  │ │
│ │ 2024-01-15...   │ │
│ └───────────────────┘ │
└─────────────────────────┘
```

### 配置界面
```
┌─────────────────────────┐
│ 错误关键字配置      │
│                     │
│ 关键字: [______]   │
│ 描述: [__________] │
│ [添加]              │
│                     │
│ ┌───────────────────┐ │
│ │ print_err         │ │
│ │ 该错误为打印...  │ │
│ │ [✏️] [🗑️]      │ │
│ └───────────────────┘ │
│                     │
│ ┌───────────────────┐ │
│ │ copy_err          │ │
│ │ 该错误为复制...  │ │
│ │ [✏️] [🗑️]      │ │
│ └───────────────────┘ │
│                     │
│ [导出配置]          │
└─────────────────────────┘
```

### 编辑模式
```
┌─────────────────────────┐
│ 错误关键字配置      │
│                     │
│ 关键字: [print_err]  │
│ 描述: [该错误为...]  │
│ [✓] [✗]           │
│                     │
│ ...其他条目...       │
└─────────────────────────┘
```

## 常见问题

**Q: 配置会丢失吗？**
A: 不会。配置保存在本地存储中，关闭应用后仍然保留。

**Q: 可以导出配置吗？**
A: 可以。点击「导出配置」按钮可以将配置保存为 JSON 文件。

**Q: 可以导入配置吗？**
A: 当前版本暂不支持直接导入，但可以手动编辑 JSON 文件内容。

**Q: 关键字区分大小写吗？**
A: 不区分。检测时会将日志和关键字都转换为小写进行比较。

**Q: 可以添加多个相同的关键字吗？**
A: 可以，但不建议。建议使用不同的关键字来区分不同的错误类型。
