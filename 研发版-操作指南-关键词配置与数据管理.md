# 操作指南：关键词配置 → 代码检索 → 数据管理

> 适用版本：v1.3.1  
> 更新日期：2026-07-17

---

## 概述

本文档介绍如何通过三个环节完成日志分析的数据准备工作：

```
关键词配置  ──→  代码检索  ──→  模块日志（JSON）
                                      ↓
                                    快速分析 ──→ 得出分析结果
                                      ↑
负责人信息  ──→   数据管理面板 ──→  模块负责人表
```

| 步骤 | 操作内容 | 产出物 |
|:----:|----------|--------|
| **一** | 配置代码检索关键词（正则模式） | 检索规则 |
| **二** | 选择源码目录，执行代码检索 | **模块日志文件**（JSON） |
| **三** | 在数据管理面板中维护模块负责人表 | **模块负责人文件**（mappings.json） |
| **四** | 导入模块日志 + 打开主日志 → 快速分析 | 匹配结果 + 负责人清单 |

---

## 第一部分：关键词配置与代码检索

### 1.1 关键词的作用

关键词（代码检索模式）定义了从源代码中**提取日志打印语句**的正则表达式规则。每条检索结果记录：
- 代码文件路径
- 行号
- 所在函数名（由 ctags 自动提取）
- 匹配到的正则模式名称
- 提取出的静态日志文本
- 关键词分词列表

### 1.2 打开关键词设置

1. 
3. 默认已内置两条模式：

| 模式名称 | 正则表达式 | 说明 |
|----------|-----------|------|
| `LOGE错误` | `LOGE\s*\(\s*"[^"]*"` | 匹配 C/C++ 的 `LOGE("xxx")` 宏 |
| `VIDEO_LOGE错误` | `VIDEO_LOGE\s*\(\s*"[^"]*"` | 匹配 C/C++ 的 `VIDEO_LOGE("xxx")` 宏 |

### 1.3 添加自定义检索模式

1. 单击 **+ 新增** 按钮
2. 填写三个字段：

| 字段 | 必填 | 说明 |
|------|:--:|------|
| **模式名称** | ✅ | 检索结果的标记名称，如 `LOG_ERROR` |
| **正则表达式** | ✅ | 用于匹配源代码中日志打印语句的正则 |
| **描述** | | 可选的说明文字 |

3. 勾选/取消勾选 **启用** 开关控制该模式是否参与检索
4. 单击 **应用** 保存

### 1.4 正则表达式编写规范

> ⚠️ **正则表达式必须包含引号捕获组**，因为 `extractPrintStaticString` 函数从引号内提取静态日志文本。

| ✅ 正确示例 | ❌ 错误示例 |
|------------|------------|
| `LOGE\s*\(\s*"[^"]*"` | `LOGE\s*\(`（缺少引号） |
| `LOG_PRINT\s*\(\s*"[^"]*"` | `"[^"]*"`（缺少宏名前缀） |

**常用模式参考：**

| 场景 | 正则表达式 |
|------|-----------|
| C/C++ LOGE  | `LOGE\s*\(\s*"[^"]*"` |

### 1.5 执行代码检索

1. 单击工具栏 **代码日志检索** 按钮（或通过设置面板中的代码检索标签页）
2. 选择检索模式：
   - **离线模式**（推荐）：基于正则，需要选择源码根目录
   - **在线模式**：调用 AI API 解析，需先在设置中配置 AI 地址和 Key
3. 单击 **🔍 开始检索**（离线）或 **🤖 AI 检索**（在线）
4. 系统弹出文件夹选择对话框 → 选择源码根目录
5. 等待检索完成（进度通过"正在检索代码日志…"遮罩提示）

**检索流程内部过程：**
```
选择源码目录
  ↓
ctags -R 扫描全目录，构建函数索引（3-8秒）    
  ↓ 若 ctags 不可用，自动降级为正则扫描
walkDirectoryAsync() 递归收集代码文件（≤2000个）
  ↓
processFile() × N 分批并发（每批20个）
  ├─ 正则逐行匹配
  ├─ ctags 索引 O(1) 查表获取函数名
  └─ extractPrintStaticString() 提取静态文本
  ↓
返回结果（≤5000条）
```

### 1.6 查看检索结果

检索完成后，在代码日志检索面板中显示结果列表，每条包含：

```
📄 代码文件: C:\project\src\video_pipeline.cpp
   行: 649
   函数: video_pcie_cancel_process    ← ctags 自动标注
   模式: LOGE错误
   文本: CANCEL
```

### 1.7 导出检索结果

> ⚠️ 检索结果仅在内存中，关闭应用后丢失。**必须导出为 JSON 文件**以持久化。

1. 在代码日志检索面板中单击 **📤 导出** 按钮
2. 选择保存路径，文件名建议以模块名命名（如 `video_pipeline.json`）
3. 导出格式示例：

```json
{
  "codeSearchResults": [
    {
      "codeFile": { "fileName": "C:\\project\\src\\video_pipeline.cpp" },
      "line": 649,
      "functionName": "video_pcie_cancel_process",
      "matchedPattern": "LOGE错误",
      "matchedText": "CANCEL"
    }
  ],
  "exportedAt": "2026-07-17T10:00:00.000Z"
}
```

### 1.8 将检索结果导入为模块日志

1. 单击工具栏 **📥 导入配置** 按钮
2. 在弹窗中输入**项目名称**（如 `PrinterFirmware`），若项目不存在则自动创建
3. 选择导入模式：
   - **合并导入**：保留已有数据，追加新数据
   - **覆盖导入**：清空后替换
4. 单击 **导入模块日志配置** 按钮
5. 在文件选择器中选中之前导出的 JSON 文件（支持多选）
6. 导入成功后，数据存储到 `config/<项目名>/code-search/` 目录下

---

## 第二部分：数据管理与模块负责人表

### 2.1 模块负责人表的作用

模块负责人表是一张 **代码路径 → 模块名称 → 负责人** 的映射表。每条记录包含：

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `codePath` | 字符串 | 源文件路径片段（用于路径匹配） | `3588/video` |
| `moduleName` | 字符串 | 业务模块名称 | `视频 (VIDEO)` |
| `contactName` | 字符串 | 模块负责人姓名 | `刘强` |

快速分析时，系统将匹配到的代码文件路径与 `codePath` 做**路径段匹配**（`/` 边界检查），找到对应负责人。

### 2.2 打开数据管理面板

1. 单击工具栏 **🔄 数据管理** 按钮
2. 在面板顶部确认/选择当前**项目**（下拉选择器）
3. 面板有两个标签页：
   - **模块负责人表**：管理 codePath → moduleName → contactName 映射
   - **模块日志表**：管理已导入的模块日志文件

### 2.3 手动添加映射记录

1. 在模块负责人表标签页，单击 **+ 新增** 按钮
2. 在新增的空行中直接录入：

| 字段 | 填写要求 |
|------|----------|
| 代码路径 | 文件的路径片段，如 `3588/video` |
| 模块名称 | 可读的业务模块名，如 `视频 (VIDEO)` |
| 负责人 | 负责人姓名，如 `刘强` |

3. 修改自动保存，无需额外操作

### 2.4 批量导入映射表

**方式一：数据管理面板直接导入**

1. 在模块负责人表标签页，单击 **+ 导入** 按钮
2. 选择 JSON 文件 → 数据暂存到"待合并"区域
3. 单击 **合并（N）** 按钮 → 数据合并到当前项目

**方式二：通过导入配置弹窗**

1. 单击工具栏 **📥 导入配置** → 输入项目名称 → 选择模式
2. 单击 **导入模块负责人配置**

**映射表 JSON 格式规范：**

```json
{
  "version": "2.0",
  "mappings": [
    {
      "codePath": "3588/video",
      "moduleName": "视频流水线 (VIDEO)",
      "contactName": "刘强"
    },
    {
      "codePath": "3588/scan",
      "moduleName": "扫描解析 (SCAN_MFP)",
      "contactName": "李娜"
    }
  ]
}
```

### 2.5 编辑和删除

| 操作 | 方法 |
|------|------|
| 编辑单元格 | 直接点击表格中的 **代码路径** / **模块名称** / **负责人** 字段，修改自动保存 |
| 删除单条 | 单击该行右侧的 **删除** 按钮 |
| 一键清空 | 单击 **一键清空** 按钮（需二次确认） |
| 导出备份 | 单击 **📤 导出** 按钮 |

### 2.6 模块日志表管理

在"模块日志表"标签页可以：

| 操作 | 说明 |
|------|------|
| 按文件分组查看 | 每个 JSON 文件为一个分组，单击 ▶ 展开查看逐条明细 |
| 编辑条目 | 展开后可编辑：代码文件、行号、函数名、匹配模式、匹配文本 |
| 新增条目 | 单击 **+ 新增条目** 手动追加 |
| 删除条目 | 单击逐行 **删除** |
| 删除整个文件 | 单击分组头部的 **删除整个文件** |
| 导出单个文件 | 单击 **📤 导出** |
| 搜索过滤 | 顶栏搜索框支持文件名和条目内容全字段匹配 |

---

## 第三部分：关键注意事项

### 3.1 正则表达式注意事项

| 注意点 | 说明 |
|--------|------|
| **必须含引号** | 正则必须匹配引号内容，否则 `extractPrintStaticString` 无法提取文本 |
| **避免过宽匹配** | 如 `error` 的正则会匹配到 `errorCount`、`isError` 等，建议用 `LOGE\s*\(` 限定宏名 |
| **转义反斜杠** | JSON 中 `\s` 需写成 `\\s`，UI 界面中直接填写 `\s` |
| **大小写** | 正则默认大小写敏感，如需不敏感可加 `(?i)` 前缀 |
| **启用/禁用** | 每次检索只使用勾选启用的模式，无需删除未使用的模式 |

### 3.2 检索边界限制

| 限制项 | 默认值 | 说明 |
|--------|:---:|------|
| 最大文件数 | 2000 | 超过后不再扫描子目录 |
| 最大结果数 | 5000 | 超过后停止处理 |
| 每批并发数 | 20 | 控制磁盘 I/O 压力 |
| ctags 超时 | 60 秒 | 超过时间降级为正则扫描 |
| ctags 输出上限 | 50 MB | 超过降级为正则 |
| 函数名扫描范围 | 500 行 | 仅正则降级时有效（ctags 为精确索引无此限制） |


### 3.3 代码路径匹配规则

`codePath` 与检索结果中的代码文件路径做**路径段匹配**：
- `project\src` 可匹配 `C:\project\src\video_pipeline.cpp`
- `src/video_pipeline.cpp` 可匹配 `C:\project\src\video_pipeline.cpp`
- 边界检查：`/video_pipeline.cpp` 不会匹配 `/video_pipeline.cpp.bak`

### 3.4 数据一致性要求

| 要求 | 说明 |
|------|------|
| **映射表完整性** | 快速分析时若找不到对应负责人，会提示"请检查映射表配置" |
| **模块日志 functionName** | ctags 提取的函数名与运行时日志中的函数名必须一致（含 `::` 限定名），否则无法匹配 |
| **JSON 格式** | 导入的 JSON 支持 `{ codeSearchResults: [...] }` 包装或纯 `[...]` 数组 |
| **项目隔离** | 每个项目的数据独立存储在 `config/<项目名>/` 下，互不干扰 |

### 3.5 异常处理与常见问题

| 现象 | 可能原因 | 解决方案 |
|------|----------|----------|
| 检索结果 functionName 为空 | ctags 未安装或源码目录无函数 | 检查 `resources/ctags/ctags.exe` 是否存在，或尝试对单个文件测试 |
| 检索无结果 | 正则模式不匹配实际代码 | 在源代码中手动搜索确认宏名，调整正则 |
| 快速分析无匹配 | 模块日志未导入或等级不是 ≥ERROR | 确认模块日志已导入当前项目，确认运行时日志含 `[ERROR]` 行 |
| 找不到负责人 | codePath 配置不一致 | 检查映射表的 codePath 与检索结果的文件名是否路径段匹配 |
| 导入后数据不显示 | 项目未切换 | 检查数据管理面板顶部当前项目是否正确 |
| ctags 降级提示 | ctags.exe 路径错误 | 开发模式检查 `resources/ctags/ctags.exe`，生产模式检查 exe 同级 `ctags/` 目录 |


---
