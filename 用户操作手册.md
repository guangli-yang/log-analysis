# 日志分析工具 - 用户操作手册

## 简介

日志分析工具是一款功能强大的跨平台桌面应用程序，专为高效查看和分析日志文件而设计。

## 系统要求

- Windows 10/11 及以上操作系统
- 至少 2GB 可用内存（推荐 4GB+）
- 100MB 可用磁盘空间

## 安装说明

1. 下载安装包 `Log-Analyzer-Setup-x.y.z.exe`
2. 双击运行安装程序
3. 按照安装向导完成安装
4. 从桌面或开始菜单启动应用

### 便携版使用

直接运行 `Log-Analyzer-x.y.z.exe` 即可，无需安装。

## 功能说明

### 1. 文件操作

#### 打开单个日志文件
- 点击工具栏的「打开文件」按钮
- 在文件选择对话框中选择日志文件（支持 .log, .txt, .log.1, .log.2 等格式）
- 文件内容将显示在主窗口中

#### 打开文件夹
- 点击工具栏的「打开文件夹」按钮
- 选择包含日志文件的文件夹
- 文件夹内所有支持的日志文件将被自动加载
- 使用顶部的下拉菜单在文件间切换

#### 历史记录
- 点击「历史记录」按钮查看最近打开的文件
- 点击历史记录中的项目可快速重新打开
- 可清空历史记录

### 2. 日志查看

#### 基本操作
- 使用鼠标滚轮或拖拽滚动条浏览内容
- 双击任意行可复制该行内容
- 行号显示在左侧

#### 字体调整
- 点击工具栏的「A-」和「A+」按钮调整字体大小
- 字体大小范围：8px - 32px

### 3. 搜索功能

#### 基本搜索
- 在右上角搜索框输入搜索内容
- 搜索结果会自动高亮显示
- 实时显示匹配数量

#### 搜索选项
- **区分大小写**：勾选后搜索时区分大小写
- **全字匹配**：只匹配完整单词
- **正则表达式**：使用正则表达式进行高级搜索

#### 导航搜索结果
- 点击「↑」和「↓」按钮在搜索结果间跳转
- 快捷键：Enter（下一个）、Shift+Enter（上一个）
- 当前匹配项会以蓝色高亮显示

### 4. 错误分析

#### 自动检测
- 应用会自动扫描日志中的错误关键字
- 检测到错误时，右侧错误分析面板会自动展开
- 每个错误会显示：行号、关键字、描述信息和上下文

#### 错误关键字配置
- 点击错误分析面板顶部的「⚙️」按钮进入配置模式
- 添加新的错误关键字和对应的描述信息
- 删除不需要的错误关键字
- 格式示例：
  - 关键字：`print_err`
  - 描述：`该错误为打印模块，请找打印团队分析`

#### 导出配置
- 在配置模式下点击「导出配置」
- 选择保存位置，将错误关键字配置导出为 JSON 文件

### 5. 面板控制

#### 历史记录面板
- 点击「历史记录」按钮可显示/隐藏左侧历史记录面板
- 点击「×」按钮关闭面板

#### 搜索面板
- 点击搜索面板标题可折叠/展开搜索面板
- 折叠时只显示标题栏

#### 错误分析面板
- 点击错误分析面板标题可折叠/展开该面板
- 折叠时只显示窄条标题栏

## 性能说明

- 支持打开 100MB 以上的大型日志文件
- 使用虚拟滚动技术，确保大文件浏览流畅
- 搜索响应时间通常在 1 秒以内

## 常见问题

**Q: 支持哪些日志文件格式？**
A: 支持 .log, .txt, .log.1, .log.2, .log.3 等格式，也支持无扩展名的纯文本文件。

**Q: 打开大文件很慢怎么办？**
A: 应用已针对大文件优化，通常 100MB 文件可在 5 秒内打开。如遇性能问题，请确保系统有足够内存。

**Q: 如何导出搜索结果？**
A: 目前版本暂不支持直接导出搜索结果，但可以通过复制文本的方式保存。

**Q: 错误分析面板会自动展开吗？**
A: 是的，当检测到错误关键字时，错误分析面板会自动展开。

## 技术支持

如有问题或建议，请联系开发团队。

## 文件夹批量快速分析

### 功能概述

文件夹批量快速分析允许您一次性选择整个文件夹中的日志文件，使用当前项目的模块数据对所有文件进行批量匹配分析，并自动生成分析结果文件。

### 使用步骤

1. **打开快速分析面板**：在工具栏点击「快速分析」按钮（🔗 图标）
2. **导入文件夹**：在面板顶部点击「📁 导入日志文件夹」按钮
3. **选择目标文件夹**：在系统对话框中选中包含日志文件的文件夹
4. **选择要分析的文件**：在弹出的文件列表中勾选需要分析的文件（默认全选所有支持的文件）
5. **开始分析**：点击「开始分析（N 个文件）」按钮
6. **等待完成**：进度面板会实时显示每个文件的分析状态
7. **查看结果**：分析完成后可在面板下拉框中切换查看各文件的分析结果

### 支持的文件格式

- `.log` — 标准日志文件
- `.txt` — 纯文本日志
- `.out` — 输出日志
- `.err` — 错误日志
- `.log.N` — 轮转日志（如 `.log.1`、`.log.2`）

不支持的格式会在结果文件中标注"⛔ 该文件格式不支持，跳过分析"。

### 结果文件

- **命名规则**：`原文件名_分析结果.txt`（如 `printer.log` → `printer.log_分析结果.txt`）
- **保存位置**：与原日志文件同目录
- **已存在时**：直接覆盖
- **内容格式**：
  1. 批次全局总结（文件夹路径、分析时间、文件统计、负责人概览）
  2. 该文件的详细匹配结果（按负责人分组：👤 人 → 📄 文件名·函数名 → 完整日志行）

### 前置条件

- 当前项目必须已导入**模块日志**（否则所有匹配结果为空）
- 建议已导入**模块映射表**（否则结果不含负责人信息）
- 启动分析前会弹出确认对话框提醒以上条件

### 注意事项

- 分析为**串行逐个**执行，非并行处理
- 分析过程中面板关闭按钮禁用，可通过「取消分析」中断
- 取消后已完成的结果文件**保留**，不会删除
- 分析完成后进度面板停留约 3 秒自动关闭
- 批量模式和单文件模式数据互相独立，切换不丢失
- 若负责人表中填写了**联系方式**，批量完成视图会显示 **📧 复制全部联系方式** 按钮，一键复制可直接粘贴到邮件收件人栏

## 版本历史

### v1.0.0
- 初始版本发布
- 支持基本的日志查看和搜索功能
- 支持错误智能分析
