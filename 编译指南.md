# Log Analyzer 编译指南

## 环境要求

### 1. Node.js 环境
- **Node.js 版本**: 18.x 或更高版本
- **npm 版本**: 9.x 或更高版本

### 2. 系统要求
- **操作系统**: Windows 10/11 (64位)
- **内存**: 至少 4GB RAM
- **磁盘空间**: 至少 2GB 可用空间

### 3. 必需软件
- Node.js (https://nodejs.org/)
- npm (随 Node.js 一起安装)

---

## 快速开始

### 步骤1: 安装 Node.js

1. 访问 https://nodejs.org/
2. 下载 LTS 版本（推荐 18.x 或更高版本）
3. 运行安装程序并按照提示完成安装

### 步骤2: 安装依赖

在项目根目录下打开终端，运行以下命令：

```bash
npm install
```

这将安装项目所需的所有依赖包。

### 步骤3: 开发模式运行

```bash
npm run dev
```

这将同时启动 Vite 开发服务器和 Electron 应用。

### 步骤4: 构建生产版本

```bash
npm run dist
```

构建完成后，可执行文件将位于 `release-v0.2.1` 目录下。

---

## 可用脚本命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发模式（推荐） |
| `npm run dev:vite` | 仅启动 Vite 开发服务器 |
| `npm run dev:electron` | 仅启动 Electron 应用 |
| `npm run build` | 构建前端和 Electron 代码 |
| `npm run build:vite` | 仅构建前端代码 |
| `npm run build:electron` | 仅构建 Electron 代码 |
| `npm run pack` | 打包为 unpacked 目录 |
| `npm run dist` | 构建可执行安装包 |

---

## 编译输出

执行 `npm run dist` 后，会在项目根目录生成以下文件：

```
release-v0.2.1/
├── Log Analyzer 0.2.0.exe          # 便携版（可直接运行）
├── Log Analyzer Setup 0.2.0.exe    # 安装程序
└── win-unpacked/                   # 解压版
    └── Log Analyzer.exe
```

---

## 常见问题

### Q1: npm install 失败
**解决方案**: 
- 清除 npm 缓存: `npm cache clean --force`
- 使用国内镜像: `npm config set registry https://registry.npmmirror.com`

### Q2: 构建时提示文件被占用
**解决方案**: 
- 关闭正在运行的 Log Analyzer 应用
- 关闭杀毒软件或将其添加到信任列表
- 删除 build 目录后重试

### Q3: Electron 启动失败
**解决方案**:
- 确保已运行 `npm install`
- 检查 Node.js 版本是否满足要求

### Q4: TypeScript 编译错误
**解决方案**:
- 确保 TypeScript 已正确安装
- 运行 `npx tsc --noEmit` 查看具体错误

---

## 一键编译脚本

项目提供了以下批处理脚本，方便快速编译：

### 编译脚本列表
- `启动开发模式.bat` - 启动开发模式
- `打包应用.bat` - 构建生产版本
- `运行应用.bat` - 运行已构建的应用

---

## 技术栈

- **前端框架**: React 18
- **构建工具**: Vite 5
- **桌面框架**: Electron 28
- **语言**: TypeScript
- **样式**: CSS (支持深色/浅色主题)

---

## 联系方式

如有问题，请联系开发者。
