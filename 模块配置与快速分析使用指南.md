# 模块配置与快速分析使用指南

## 目录

- [一、概念说明](#一概念说明)
- [二、创建项目](#二创建项目)
- [三、生成并导入模块日志文件](#三生成并导入模块日志文件)
- [四、设置模块负责人表](#四设置模块负责人表)
- [五、执行快速分析](#五执行快速分析)
- [六、数据管理](#六数据管理)
- [七、文件结构与格式](#七文件结构与格式)
- [八、常见问题排查](#八常见问题排查)

---

## 一、概念说明

| 名词 | 是什么 | 作用 |
|------|--------|------|
| **模块日志文件** | 从源代码中检索出的日志打印语句集合。每条包含代码文件路径、行号、函数名、匹配模式、提取文本 | 快速分析时用于将日志行与源代码位置关联 |
| **模块负责人表** | 代码路径 → 模块名称 → 负责人 的映射关系 | 快速分析匹配成功后自动显示责任人 |

### 工作流程

```
源代码目录
    │
    ▼
[代码检索] 扫描匹配 LOG 打印语句 → 代码搜索结果
    │
    ▼
[导入配置] 选择项目 → 导入模块日志配置 → 存入 config/<项目>/code-search/*.json
                                                 │
[数据管理/导入配置] 导入或手工编辑               │
    模块负责人表 → 存入 config/<项目>/module-mapping/mappings.json
                                                 │
[打开日志文件]                                    │
    │                                            │
    ▼                                            ▼
[快速分析] 日志行 L88: ... ←→ 模块日志(行号+关键词) ←→ 模块负责人表(路径匹配)
    │
    ▼
  匹配结果 + 责任人
```

---

## 二、创建项目

1. 顶部工具栏 → 项目下拉选择器（🗂️ 图标左侧） → 单击 **＋** 按钮
2. 输入项目名称（如 `MyProject`）
3. 项目自动创建在 `config/MyProject/` 目录下，包含 `code-search/` 和 `module-mapping/` 两个子目录

> 多个项目间的模块日志和负责人表互不干扰，可随时通过下拉菜单切换。

---

## 三、生成并导入模块日志文件

### 步骤 1：代码检索

1. 单击工具栏 **📊 分析** → 切换到 **代码检索** 标签页
2. 选择 **离线** 模式
3. 单击 **开始检索**
4. 在弹出的文件夹选择器中，选择你的**源代码根目录**
5. 系统使用预定义的正则表达式（可在 **⚙️ 关键词设置 → 代码检索** 中自定义）逐文件扫描匹配行
6. 对每条匹配行（如 `LOGE("OPEN %s falie\n", flie)`），自动提取引号内静态文本、去除 `%s` `%d` `\n` `{...}` 等格式符

### 步骤 2：导出结果

检索完成后，结果面板中点击 **导出** 按钮，保存为 JSON 文件。

> **默认模式**：`LOGE\s*\(\s*"[^"]*"` —— 匹配所有 LOGE 调用
> 
> 可在 **⚙️ 关键词设置 → 代码检索** 中增删模式（如 `VIDEO_LOGE`、自定义宏名）

### 步骤 3：导入到项目

1. 单击工具栏 **📥 导入配置**
2. 输入**项目名称**（与创建时一致，支持从已有列表下拉选择）
3. 选择导入模式：
   - **合并导入**（推荐）：保留已有数据，追加新数据
   - **覆盖导入**：清空当前项目所有模块日志，用新数据替换
4. 单击 **📄 导入模块日志配置**
5. 选择步骤 2 导出的 JSON 文件

> 也可直接导入他人分享的 JSON 文件，无需先做代码检索。

### 条目结构

每条代码检索结果包含以下字段：

| 字段 | 说明 | 示例 |
|------|------|------|
| `codeFile.fileName` | 源代码文件完整路径 | `C:\project\network.c` |
| `line` | 代码行号 | `88` |
| `functionName` | 所在函数名 | `sendData` |
| `matchedPattern` | 匹配到的正则名称 | `LOGE错误` |
| `matchedText` | 提取的静态文本 | `Network send error` |
| `keywords` | 分词后的关键词数组 | `["Network","send","error"]` |

---

## 四、设置模块负责人表

模块负责人表有**两种创建方式**。

### 方式一：数据管理面板（手工编辑）

1. 单击工具栏 **数据管理** 按钮
2. 确认顶部项目选择器为当前项目
3. 切换到 **模块负责人表** 标签页
4. 点击 **+ 新增** 按钮，逐行填写：
   - **代码路径**：用于匹配代码文件路径的关键路径段（如 `project\2`，注意路径分隔符）
   - **模块名称**：可读模块名（如 `网络模块`）
   - **负责人**：模块负责人姓名（如 `王五`）
   - **联系方式**（可选）：邮箱或电话号码，填写后可在快速分析结果中一键复制
5. 可直接点击任意单元格修改内容（含联系方式列），修改自动保存
6. 支持删除和**一键清空**（需二次确认）

### 方式二：导入 JSON（批量导入）

准备一个 JSON 文件：

```json
{
  "version": "2.0",
  "mappings": [
    { "codePath": "project\\1", "moduleName": "音频模块", "contactName": "张三", "contactInfo": "zhangsan@example.com" },
    { "codePath": "project\\2", "moduleName": "网络模块", "contactName": "王五", "contactInfo": "wangwu@example.com" },
    { "codePath": "project\\3", "moduleName": "解码模块", "contactName": "李四" }
  ]
}
```

导入步骤：
1. 工具栏 → **📥 导入配置**
2. 输入项目名称
3. 单击 **📋 导入模块负责人配置**
4. 选择 JSON 文件

> **关键**：`codePath` 必须是代码文件路径中的一段完整路径片段。
> - `project\2` 可以匹配 `C:\project\2\network.c`
> - 但不能匹配 `C:\project\22\network.c`（22 ≠ 2 路径片段）

---

## 五、执行快速分析

### 前提条件

1. ✅ 已创建项目并导入模块日志
2. ✅ 已配置模块负责人表
3. ✅ 打开了一个日志文件

### 操作步骤

1. 单击工具栏 **🔗 快速分析** 按钮
2. 勾选要参与匹配的模块（可多选）
3. 单击 **🔍 开始匹配**

### 匹配规则

系统对日志文件中的每一行执行以下匹配：

```
日志行格式要求：L<源代码行号>: <日志内容>
例：L88: ERROR: Network send error
```

匹配逻辑：
1. **行号精确匹配**：日志行号 `88` 必须等于代码检索结果中的 `line: 88`
2. **关键词分词验证**：提取文本分词后所有关键词必须在日志内容中出现（大小写不敏感）
   - 如：提取文本 `"Network send error"` → 关键词 `["Network","send","error"]`
   - 日志 `"ERROR: network send error"` 包含全部三个词 → ✅ 匹配

### 匹配结果

- ✅ 匹配成功 → 按**负责人**分组展示，每项包含：代码文件名、函数名、匹配日志行详情
- ❌ 未匹配 → 显示"未在日志中找到匹配"或"未找到对应的模块负责人"
- 无映射的匹配结果归入 **未分配负责人** 分组
- 若负责人表中填写了**联系方式**，可通过 **📧 复制联系方式** 按钮一键复制（直接粘贴到邮件收件人）

### 模块负责人关联规则

匹配到的代码文件路径片段若与模块负责人表中的 `codePath` 完全匹配（按目录层级），则自动关联该模块和负责人。

---

## 六、数据管理

工具栏 **数据管理** 按钮打开管理面板：

### 模块负责人表标签

| 操作 | 方式 |
|------|------|
| 查看 | 表格滚动浏览，搜索框输入过滤（支持代码路径、模块名称、负责人、联系方式） |
| 新增 | 点击 + 新增 → 填写后自动保存 |
| 编辑 | 直接点击任意单元格修改内容（含联系方式列） |
| 删除 | 点击行的删除按钮 |
| 清空 | 点击一键清空（需确认） |

### 模块日志表标签

| 操作 | 方式 |
|------|------|
| 查看 | 默认按文件分组折叠，点击文件头 ▶ 展开 |
| 编辑条目 | 展开后直接修改代码文件/行号/函数名/匹配模式/匹配文本 |
| 新增条目 | 展开分组后点击 + 新增条目 |
| 删除条目 | 点击条目行的删除按钮 |
| 删除文件 | 点击分组头的删除整个文件按钮 |
| 搜索 | 输入关键词自动展开匹配分组，模糊匹配所有字段 |

> **全部展开/全部折叠**按钮可快速切换所有分组的显示状态。

---

## 七、文件结构与格式

### 项目目录结构

```
config/
└── <项目名>/
    ├── code-search/              # 模块日志目录
    │   ├── network.json          # 每个 JSON 文件对应一个模块
    │   ├── audio.json
    │   └── decoder.json
    └── module-mapping/           # 模块负责人表目录
        └── mappings.json         # 单文件存储所有映射关系
```

### 模块日志 JSON 格式

```json
[
  {
    "codeFile": { "fileName": "C:\\project\\2\\network.c" },
    "line": 88,
    "functionName": "sendData",
    "matchedPattern": "LOGE错误",
    "matchedText": "Network send error",
    "keywords": ["Network", "send", "error"]
  },
  {
    "codeFile": { "fileName": "C:\\project\\1\\audio.cpp" },
    "line": 200,
    "functionName": "playAudio",
    "matchedPattern": "LOGE错误",
    "matchedText": "Audio buffer underrun",
    "keywords": ["Audio", "buffer", "underrun"]
  }
]
```

> `keywords` 字段为空或不存在的旧数据也能正常工作——系统会自动从 `matchedText` 动态提取关键词。

### 模块负责人 JSON 格式

```json
{
  "version": "2.0",
  "mappings": [
    { "codePath": "project\\1", "moduleName": "音频模块", "contactName": "张三", "contactInfo": "zhangsan@example.com" },
    { "codePath": "project\\2", "moduleName": "网络模块", "contactName": "王五", "contactInfo": "wangwu@example.com" }
  ]
}
```

> `contactInfo` 字段为可选，填写联系方式后可在快速分析结果中一键复制。

---

## 八、常见问题排查

### Q1：快速分析提示"请先导入模块映射表"

→ 尚未配置模块负责人表。按[第四节](#四设置模块负责人表)操作导入或手工添加。

### Q2：日志行有匹配但快速分析无结果

→ 检查日志行格式是否为 `L<行号>: <内容>`（如 `L88: OPEN myfile falie`）。注意 `L` 和冒号均不可省略。

### Q3：匹配到了但负责人不显示

→ 检查负责人表的 `codePath` 是否与代码文件路径中的某一层完整路径匹配：

| 代码文件路径 | `codePath` 正确写法 | 错误写法 | 原因 |
|-------------|-------------------|---------|------|
| `C:\project\2\network.c` | `project\2` | `project` | `project` 后面是 `\2`，不是路径分隔符 |
| `/home/user/proj/audio.cpp` | `proj` | `user/proj` | 必须从路径片段边界开始匹配 |

### Q4：多人应该匹配但只有一人出现

→ 打开开发者工具（`Ctrl+Shift+I` → Console），执行快速分析后查看日志：

```
[快速分析] matchedCodePaths: [...]
[快速分析] codePath="..." → matched N mapping(s)
```

- 如果 `matchedCodePaths` 只有 1 个 → 可能同条目 `codeFile.fileName` 字段丢失
- 如果显示 `matched 0 mapping(s)` → `codePath` 与映射表不匹配，调整路径

---

> **版本**: v1.3.0+  
> **更新日期**: 2026-07-14
